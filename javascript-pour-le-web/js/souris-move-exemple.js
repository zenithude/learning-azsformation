const doc = document.getElementsByTagName("body")

doc.addEventListener('mousemove', (event) => {
    const x = event.offsetX; // Coordonnée X de la souris dans le corps de page "body"
    const y = event.offsetX; // Coordonnée Y de la souris dans le corps de page "body"
})