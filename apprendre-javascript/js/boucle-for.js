/**
 * Nous déclarons entre parenthèse :
 * - La variable compteur
 * - La condition de poursuite de la boucle
 * - L'incrémentation de la variable
 * 
 * Ce sont des instructions elles sont donc séparées par des points virgules
 *  
 */

for (let compteur = 0; compteur <= 10; compteur++) {
    console.log(`Notre variable compteur vaut : ${compteur}`);
}